# A document that emits its Plan

This is an ordinary document. It writes `<Plan>` with no `as`, which is the bare
form: the approved program's source is emitted here, where the component is
written.

<Plan session="planner">Write the release program.</Plan>
