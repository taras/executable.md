# Notes

Ordinary Markdown. Running it fails, which is how a suite proves it was left
alone under the default pattern.

<Test name="ordinary markdown ran">
<Assert expr={false} msg="notes.md should not have executed" />
</Test>
