# A document that stands on nothing

It asserts about itself only, so it holds against any fixture. Observed after a
failing document to show that the failure stayed in the session that recorded
it.

<Test name="a document observed after a failure still passes">
<AssertEquals actual={1} expected={1} />
</Test>
