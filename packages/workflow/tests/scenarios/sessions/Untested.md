# A document with no tests

It renders an assertion report and discovers no test, which is the shape a
silently skipped suite takes. The session settles `Err` for it.

<AssertEquals actual={1} expected={1} />
