---
emoji: "💡"
---

{meta.emoji} **Formatting tip:** Use `<Content />` inside your component
body to mark where the caller's children appear. If your component doesn't
include `<Content />`, children are silently discarded.
