# Document targets

This document addresses two sections. `xmd run … --help` describes them, and
`xmd run` executes exactly one of them — its sibling does not run at all.

## Alpha

ALPHA_DESCRIBED as the first section.

ALPHA_RAN

## Beta

BETA_RAN
