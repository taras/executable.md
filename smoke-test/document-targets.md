# Document targets

This document addresses two sections. `xmd targets` lists them, and `xmd run`
executes exactly one of them — its sibling does not run at all.

## Alpha

ALPHA_RAN

## Beta

BETA_RAN
